query Input {
  cart {
    lines {
      id
      cost {
        subtotalAmount {
          amount
        }
      }
    }
  }
  discount {
    discountClasses
    metafield(namespace: "$app:discount-function", key: "config") {
      value
    }
  }
}
