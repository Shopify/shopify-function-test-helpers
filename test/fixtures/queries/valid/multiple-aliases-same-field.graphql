query Query {
  data {
    metafield1: metafield(namespace: "$app:config", key: "setting1") {
      type
      value
    }
    metafield2: metafield(namespace: "$app:config", key: "setting2") {
      type
      value
    }
    items {
      id
      count
    }
  }
}
