query Query {
  data {
    items {
      id
      count
      nonExistentField
      details {
        id
        name
        anotherInvalidField
      }
    }
    metadata {
      email
      invalidMetadataField
    }
  }
}